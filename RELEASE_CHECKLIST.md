# 发布前检查清单

- [ ] 删除 `relay.log`、本地配置、systemd 单元和编译产物
- [ ] 确认默认模式仍拒绝非 `/v1` 路径
- [ ] 确认 `--passthrough` 能转发 `/responses`、`/models`
- [ ] 运行 `go test ./...`
- [ ] 使用哑上游验证路径、请求头和请求体透明转发
- [ ] 使用测试凭证完成一次真实只读端到端请求；不要提交凭证
- [ ] README 写明订阅模式、风险边界和回滚方法
- [ ] 检查 `git diff`，确认没有 token、账号标识、日志或个人路径
